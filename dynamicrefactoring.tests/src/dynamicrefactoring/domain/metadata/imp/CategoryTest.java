package dynamicrefactoring.domain.metadata.imp;

import org.junit.Test;

public class CategoryTest {

	private static final String CATEGORY_NAME = "nombre";

	/**
	 * Comprueba que se crea correctamente la categoria.
	 */
	@Test
	public void createSimpleCategoryTest() {
		/*
		 * SimpleClassification category = new
		 * SimpleClassification(CATEGORY_NAME, CatalogModule.getStubElements());
		 * assertEquals(CATEGORY_NAME, category.getName());
		 * assertArrayEquals(CatalogModule.getStubElements().toArray(), category
		 * .getSubcategories().toArray());
		 */
	}


}
